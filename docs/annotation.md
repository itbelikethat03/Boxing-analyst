# Annotation format and protocol

Status: **not yet implemented (M3).** The approved format (`meta.toml` + `events.csv` per video) and the
protocol rules (exhaustive-or-excluded, mark replays/cutaways, thrown punches only, double-annotation) are in
`docs/plan.md` §5. This file will hold the as-built specification once the loader exists.
