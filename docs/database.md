# Database

Status: **not yet implemented (M4).** The approved schema (DDL, constraints, indexes, JSONB policy, and the
evaluation of PostgreSQL vs alternatives) is in `docs/plan.md` §0 and §3. This file will hold the
as-built documentation once `migrations/0001_core.sql` exists.
